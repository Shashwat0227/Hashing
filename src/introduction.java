public class introduction {
    public static void main(String[] args) {
        System.out.println("what is hashing ");

        // Hashing is a method of sorting and indexing data > the idea behind hashing is to allow lalrge amounts of data
        // to be indexed using keys and commonly created by formula


        System.out.println("WHY HASHING ??");

        /*
     It is time efficient in case of search operations
     performas better than oother data structure and algorithm in terms of searching operations
     TIME COMPLEXITY
     Array -o(logN)
     Linked list - O(N)
     Tree- O(logN )
     hashing - O(1)
         */
    }
}
